config.php 是公共的配置文件

# 以下配置文件动态加载,无缓存（配置文件内部用到的外部常量除外,因为用到的常量已经被缓存了）
config.change.php 是会有变化的配置,跟环境无关

config.{$mode}.php 是根据环境不同需要不同配置的配置文件

debug.php 是调试模式下才有效的配置文件

tags.php 是定义行为的配置文件,目前用来用作语言包的检测